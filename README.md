# mesh-compare
Compares "subject" STL meshes against "reference" STL meshes (single pair or batch CSV) and produces interactive Geomagic-style HTML deviation reports with signed color maps and area-weighted tolerance statistics.
